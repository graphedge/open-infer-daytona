# Daytona ↔ OpenCode Feature Matrix

Purpose: map Daytona capabilities (columns) to OpenCode components and repo artifacts (rows).
Each cell follows the schema: used | status | problem_solved | problem_remaining | example_command | evidence | migration_note

Columns (Daytona features): sandbox_lifecycle, process_control, preview_links, file_transfer, networking_egress, logs_diagnostics, model_management, cli_orchestration, provider_routing, streaming_semantics, security_handling

| Component | sandbox_lifecycle | process_control | preview_links | file_transfer | networking_egress | logs_diagnostics | model_management | cli_orchestration | provider_routing | streaming_semantics | security_handling |
|---|---|---|---|---|---|---|---|---|---|---|---|
| deploy_opencode.py | used; done; creates 2vCPU/4GB sandbox | used; done; discovers binary & forces `opencode serve` | used; in_progress; writes provider config with preview token but health checks still use wrong path | used; done; writes `/tmp/opencode.json` in sandbox | used; done; general egress ok, proxy streaming issue observed (see streaming_semantics) | used; done; greps `/tmp/opencode.log` and checks port | N/A | used; done; installs and starts OpenCode | used; in_progress; must switch checks to `GET /global/health` (see tasks/opencode-agent) | N/A (provider handles streaming) | used; done; injects `x-daytona-preview-token` into provider config (tasks/opencode-agent/tasks.md:11-15,37-41)
| extractor_status | extraction_run: basic CLI patterns found and sandbox IDs collected; evidence pointers saved to session/files/feature_matrix_extraction.log | | | | | | | | | |
| agent_client.py | No | No | used; in_progress; still targets OpenAI-style endpoints (must be rewritten) | No | used; in_progress; relies on preview proxy for outbound provider calls (tasks/api-discovery:160-168) | No | No | No | used; blocked; currently uses `/api/*` and `/v1/*` shapes (must use `/session` + `/session/{id}/message`) | used; blocked; streaming vs unary mismatch; original code assumes OpenAI semantics | used; in_progress; handles tokens but needs review (tasks/api-discovery, tasks/opencode-agent)
| serve_ollama_main.py | used; done; creates 4vCPU/8GB sandbox (ollama) | used; done; long-lived wrapper to keep `ollama serve` running | used; done; writes `OLLAMA_ENDPOINT` and `OLLAMA_TOKEN` to `ollama_info.txt` (gitignored) | used; done; streams GitHub tarball fallback into install dir | used; done; HF pull fallback due to registry/registry.ollama.ai blocked (tasks/ollama-inference) | used; done; writes logs `logs/serve_ollama-*.log` | used; done; pulls HF model `huggingface.co/bartowski/SmolLM2-135M-Instruct-GGUF` | used; done; orchestrates `ollama serve` and `ollama pull` | N/A | used; done locally; but preview proxy caused TLS resets when streaming (tasks/api-discovery:160-168) | used; done; endpoint/token saved to `ollama_info.txt` (tasks/ollama-inference)
| query_ollama.py | No | No | used; done; uses `OLLAMA_ENDPOINT` from `ollama_info.txt` to test preview/local | No | used; done; checks local preview endpoint and prints results | No | used; done; health `/api/tags` and chat `/api/chat` tested (tasks/ollama-inference) | No | No | used; done; tests streaming chat path | used; done; uses tokens from `ollama_info.txt`
| OpenCode: POST /session | No | used; done; creates session (returns id) | No | No | No | No | No | No | used; done; session creation verified (tasks/api-discovery lines 26-31) | No | No
| OpenCode: POST /session/{sessionID}/message | No | used; in_progress; OpenCode issues provider call on message send | Yes; blocked/in_progress; provider config used preview token -> earlier proxy reset; co-location workaround gave 200 empty body (tasks/api-discovery) | No | used; blocked; streaming reset observed across preview proxy (tasks/api-discovery) | used; in_progress; recommended: tail `~/.local/share/opencode/log/*.log` while issuing request | used; in_progress; depends on Ollama model state (tasks/ollama-inference) | No | used; in_progress; must use `parts` array and correct modelID (tasks/api-discovery tasks.md) | used; blocked; provider expects streaming; try `stream:false` unary or co-locate Ollama | used; done; uses preview token for preview route
| Provider plumbing (OpenCode→preview proxy→Ollama) | No | No | used; blocked; depends heavily on preview proxy semantics; TLS resets observed (tasks/api-discovery) | No | used; blocked; proxy TLS resets on streaming (tasks/api-discovery:160-168) | used; in_progress; opencode logs show ECONNRESET entries — tail during message POST | used; in_progress; provider configured to call Ollama's OpenAI-compatible `/v1` API (deploy_opencode writes config) | No | used; in_progress; provider routing exists but streaming semantics differ from OpenAI | used; blocked; streaming breaks via preview proxy; co-location works but returned empty body | used; done; uses preview token header
| Logs & diagnostics | N/A | N/A | N/A | N/A | used; done; diagnostic dumps capture proxy/TLS failures (see `ollama-postmortem.md`, tasks/api-discovery) | used; done; logs at `/tmp/opencode.log`, `~/.local/share/opencode/log/*.log`, `logs/serve_ollama-*.log` | N/A | N/A | N/A | N/A | N/A
| tasks/ docs (this repo) | N/A | N/A | used; done; tasks reference sandbox IDs & preview usage | N/A | used; done; documents proxy streaming issues and HF fallback | used; done; contains exact curl examples and file pointers (see tasks/api-discovery/tasks.md) | used; done; documents model pull strategy | N/A | used; done; documents `/doc` and required endpoint shapes | used; done; documents streaming vs unary experiments | used; done; explicitly avoids embedding secrets (tasks/housekeeping)

---

Notes & next steps
- This is an initial, hand-populated extraction from `tasks/*.md`. Cells marked `needs_validation` or `in_progress` should be validated via a light live check (GET /global/health, POST session/message with `stream:false`, tail opencode logs while sending).
- Migration notes are compact; for a fuller migration-impact report, run the validation pass and expand the `migration_note` cells into a separate CSV.

Actions to run now (if you want me to proceed):
1. Run the automated extractor to populate the above table more fully and fill `evidence` pointers precisely.
2. Optionally perform the validation pass against live sandboxes to move `needs_validation` → done/blocked.
3. Commit `tasks/feature_matrix.md` to the repo (after user review).

